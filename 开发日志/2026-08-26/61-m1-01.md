## 开发任务（首轮）

改动范围：
- `src/shared/wecir-dev/contracts.ts`：冻结 Wecir Dev 卡片领域类型、状态、错误码、分页、请求/响应和 wire 兼容策略。
- `src/shared/wecir-dev/schemas.ts`：新增严格 Zod IPC schema、危险字段拒绝、Issue/PR 编号与 card 名称校验、状态转换校验。
- `src/shared/wecir-dev/contracts.test.ts`：覆盖成功、失败、旧字段兼容、非法输入和状态转换。
- `src/shared/wecir-dev/fixtures/`：新增成功 card、失败响应、旧字段兼容 JSON fixture。
- `docs/reference/wecir-dev-card-contract.md`：文档化 V1 字段、状态、错误码、本地 execution host 限制和远程 wire 兼容边界。

改动概要：建立 Renderer、Preload、Main 唯一共享的 Wecir Dev 卡片协议，所有 IPC 数据显式携带 `schemaVersion: 1`；第一版仅允许本地 execution host；未知状态拒绝、未知非危险字段可按兼容策略忽略，新增状态/通道需能力检查。

验证：
- `pnpm exec vitest run --config config/vitest.config.ts src/shared/wecir-dev/contracts.test.ts`：6/6 通过。
- `pnpm exec oxlint src/shared/wecir-dev --deny-warnings`：通过。
- `pnpm run typecheck`：通过（Node 22 环境提示项目声明 Node 24 engine）。

commit 短哈希：92a8f4125c（回填后 amend，最终提交哈希需以 git rev-parse 为准）。
