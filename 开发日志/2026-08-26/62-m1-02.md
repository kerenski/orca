## 开发任务（首轮）

- 改动范围：`cards` 顶层视图、UI 持久化/RPC 合法值、Renderer 导航与布局、独立 Zustand slice、空状态页面、Tasks 隔离回归测试。
- 改动概要：新增独立 Cards 侧栏入口和 lazy page，以 `previousViewBeforeCards` 维持返回行为；卡片仓库选择状态与 `taskPageData`/Tasks Drawer 完全分离；持久化支持 `cards`，未知值继续回退 `terminal`；无本地 Git 仓库、GitHub CLI 缺失或未认证时显示明确空状态。
- commit 短哈希：`17cb66e545`

## 验证记录

- `pnpm tc`：通过。
- `bash skills/orca-skill/scripts/run-checks.sh --fast --json`：`typecheck=0;lint=0`，增量质量门无新问题。
- 定向导航/持久化/空状态测试：10 个文件、188 个测试通过。
- 扩展 Main/Web/Tasks Drawer/布局回归：17 个文件、233 个测试通过。
- Tasks 专项回归：58 个文件、563 个测试通过。
- `src/main/daemon/shell-ready.test.ts` 单独复跑：21 个通过、1 个跳过。

## 未解决风险

- 本地全量 Vitest 在长时间无输出后人工终止；并发阶段曾短暂报告 `shell-ready` 3 个失败，单文件复跑全部通过，由 PR CI 作最终判定。
- 本地 Electron 二进制在 postinstall 下载阶段缺失，且当前环境未提供项目要求的 `$electron` skill，未执行渲染界面/CDP 验证。
- 本地 Node.js 为 v22.23.1，仓库声明为 Node 24；已执行的类型和测试均通过，PR CI 将在标准环境复验。

## Code Review #0

- 本轮问题：首轮只有 Cards 顶层路由和空状态，缺少卡片数据连接、列表分页、创建编辑、管理动作、模板管理、分享能力，也没有证明新 Cards 实现与旧 Agent Skill 分享流隔离。
- 修复方式：新增基于共享 `wecir-dev` schema 的本地持久化数据源，返回 `WecirDevPage` 并记录 controller 指令；ready 状态按仓库加载卡片，支持 12 状态筛选/分组与分页；补齐创建、完整字段编辑、start/stop/retry/remove/refresh、永久删除、模板 CRUD/套用和按用户标识分享。仓库不存在 `AgentSkillShareCard` 组件，因此保留独立 Skills 分享 RPC，仅以新的 Cards 数据模型和隔离测试取代开发卡片复用 skill-share 的路径。
- commit 短哈希：`f25664c34c`

### 验证记录

- `pnpm tc`：通过。
- `pnpm run check:code-quality:changed`：code quality、type-aware、React Doctor 均为 0 个新问题。
- 相关 Vitest：5 个文件、37 个测试通过。
- `pnpm exec electron-vite build --mode e2e`：通过，Cards 页面进入 renderer bundle。
- `pnpm run verify:localization-extraction`：通过；同步补齐首轮 Cards 页面遗留的 19 个英文 catalog key。
- Electron Playwright 启动因固定 binary 首次下载持续无进度而终止；临时验证 spec 已删除，未留下后台进程或测试文件。
