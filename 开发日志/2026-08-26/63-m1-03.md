## 开发任务（首轮）

改动范围：Main GitHub 卡片数据适配、共享卡片数据类型、桌面 IPC、远程 Runtime RPC、Preload/Web preload、里程碑字段映射、单元测试和文档。

改动概要：新增 `getWecirDevGitHubCardData`，内部复用 `listWorkItems`、`getWorkItem`、`getWorkItemDetails`、`getPRChecks` 和 `getPRCheckDetails`；输出 Issue/PR 列表和详情字段、评论摘要、关联引用、检查详情、分页信息及逐项分类错误。补充 GitHub WorkItem 的可选 milestone 字段，并保持现有 Tasks 路由、认证、缓存、Enterprise、SSH/WSL 和 runtime 语义不变。

验证：

- `pnpm exec tsc --noEmit -p config/tsconfig.node.json --pretty false`：通过。
- `pnpm exec oxlint src/main/github/wecir-dev-card-data.ts src/main/github/wecir-dev-card-data.test.ts src/shared/wecir-dev/github-card-data.ts src/main/runtime/rpc/methods/github-repo-work-item-methods.ts src/preload/api/github-work-item-api.ts --deny-warnings`：通过。
- `pnpm exec vitest run --config config/vitest.config.ts src/main/github/wecir-dev-card-data.test.ts --reporter=dot`：2/2 通过。
- RPC 单测尝试执行，但本机 Electron 二进制下载失败（`TypeError: fetch failed`），未能启动测试套件。

未解决风险：完整 PR CI、Electron RPC 测试和跨平台运行时验证仍需 CI 环境完成；检查详情失败按设计降级为空详情，不影响同批卡片。

commit 短哈希：27df6d4973。
