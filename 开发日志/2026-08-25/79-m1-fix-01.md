## 开发任务（首轮）

### 改动范围
- `src/renderer/src/components/editor/line-copy-path.ts` - 修改路径行引用格式
- `src/renderer/src/components/editor/line-copy-path.test.ts` - 更新测试用例
- `src/renderer/src/components/tab-bar/EditorFileTabContextMenu.tsx` - 添加右键菜单项

### 改动概要

#### 1. 修改路径行引用格式（line-copy-path.ts）
- 将格式从 `path:line` 改为 `path#L{line}`（如 `file.ts#L42`）
- 符合 GitHub/GitLab 等平台的链接格式标准

#### 2. 更新测试（line-copy-path.test.ts）
- 更新测试用例以验证新的 `#L` 格式

#### 3. 添加编辑器标签右键菜单项（EditorFileTabContextMenu.tsx）
- 导入 `Hash` 图标和 `formatPathLineReference` 函数
- 从 store 获取 `editorCursorLine` 状态
- 添加 "Copy Path to Line" 菜单项（复制绝对路径加行号）
- 添加 "Copy Relative Path to Line" 菜单项（复制相对路径加行号）
- 仅在非 diff 标签时显示这些选项（通过 `!isDiff` 条件判断）
- 使用当前光标行位置（`editorCursorLine[file.id] ?? 1`）

### 功能验证点
- ✓ 行号 gutter 右键菜单已存在，使用新格式
- ✓ 编辑器标签右键菜单新增两个选项
- ✓ Diff 标签不显示 line-based 选项
- ✓ 使用 store 中跟踪的光标行位置

### commit 短哈希
e202754682

## Code Review #2

### 本轮问题
1. **editorCursorLine 可能为 undefined** - 原代码在组件顶层使用 `editorCursorLine[file.id]` 可能导致 undefined 访问错误
2. **性能浪费** - cursorLine 在组件顶层无条件计算，但仅在 !isDiff 块中使用
3. **测试 mock 缺失** - 测试文件缺少 `Hash` 图标 mock 和 `editorCursorLine` 状态 mock

### 修复方式
1. **移除组件顶层的 cursorLine 变量** - 将计算移到实际使用的地方（两个菜单项的 onSelect 回调内）
2. **添加可选链操作符** - 使用 `editorCursorLine?.[file.id] ?? 1` 防止 undefined 访问
3. **更新测试 mock** - 在 `EditorFileTabContextMenu.test.tsx` 中：
   - 添加 `Hash` 图标到 `lucide-react` mock
   - 添加 `editorCursorLine: { 'file-1': 42 }` 到 `useAppStore` mock 状态

### commit 短哈希
ee38d218a0
