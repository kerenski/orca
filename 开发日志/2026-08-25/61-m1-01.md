## 开发任务（首轮）

### 改动范围
`src/renderer/src/components/sidebar/WorktreeMetaDialog.tsx`

### 改动概要
Issue #61 要求实现的功能已在代码库中完成：

1. **Enter键提交功能**：已在 `handleCommentKeyDown` 函数（第262-272行）中实现
   - 检测纯Enter键（无修饰键）触发保存
   - 支持 Shift+Enter 插入换行（不拦截）
   - 额外支持平台特定的提交快捷键（通过 `isScreenSubmitShortcut`）

2. **提示文本**：已在textarea下方显示（第382-392行）
   - 完整提示："Supports **markdown** — bold, lists, `code`, links. Press Enter or {submitShortcutLabel} to save, Shift+Enter for a new line."
   - 包含markdown支持说明和快捷键指引

3. **Oxlint curly规则**：经检查，文件中无curly违规

### 实现验证
- `handleCommentKeyDown` 正确处理 Enter 和 Shift+Enter
- 提示文本已正确显示在comment textarea下方
- 无需额外代码改动

### Commit 短哈希
功能已包含在 PR #61 的 merge commit: `67aa2af2f3`

当前分支 `kerenski/m1-01` 已包含此功能实现。
